# Env definition file for creating environment via conda

name: cmadocs-env
channels:
 - conda-forge
dependencies:
 - black
 - sphinx
 - sphinx_rtd_theme
 - sphinx-argparse
 - sphinx-gallery
 - pip

 - pip:
   - sphinxcontrib-mermaid
